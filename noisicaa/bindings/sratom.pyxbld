# -*- mode: python -*-

import os
import os.path

import pkgconfig

def make_ext(modname, pyxfilename):
    from distutils.extension import Extension
    return Extension(
        name = modname,
        sources=[pyxfilename],
        **pkgconfig.parse('sratom-0'),
    )
